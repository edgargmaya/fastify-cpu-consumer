{
  "name": "fastify-cpu-consumer",
  "version": "1.0.0",
  "private": true,
  "description": "Single-endpoint Fastify app that burns CPU and RAM for KEDA tests.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fastify": "^5.6.1"
  }
}
